version: '3'

services:
  open-runtimes-node:
    container_name: open-runtimes-node
    build:
      context: .
    ports: 
      - 3000:3000
    environment:
      - INTERNAL_RUNTIME_KEY
    volumes:
      - ./example:/usr/example-code:ro
    command: sh /usr/local/src/example.sh