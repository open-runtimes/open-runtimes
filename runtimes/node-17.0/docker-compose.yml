version: '3'

services:
  open-runtimes-node:
    container_name: open-runtimes-node
    build:
      context: .
    ports: 
      - 6001:3000
    environment:
      - INTERNAL_RUNTIME_KEY
    volumes:
      - ./example:/usr/test-code:ro
    command: sh /usr/local/src/test.sh